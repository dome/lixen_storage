// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("jsonschema", "for jsonschema support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("jsonschema", "enable jsonschema support", "no");

if (PHP_JSONSCHEMA != "no") {
	EXTENSION("jsonschema", "jsonschema.c");
}

